format_version = 2
name = "ccc-glibc-like-header-fixture"
revision = 2
origin = "original-project-test-data"
license = "Apache-2.0 WITH LLVM-exception"
entrypoint = "glibc-like/probe.c"
compatibility_profile = "gcc-4.2.1"
certified_through = "parsing"
modeled_surfaces = ["preprocessing", "gnu-declarations"]
parser_requirements = [
    "gnu-alternative-keywords",
    "gnu-attribute-specifiers",
    "gnu-declaration-asm-labels",
    "gnu-extension-marker",
    "gnu-restrict-qualifiers",
    "gnu-typeof",
]
expected_files = [
    "glibc-like/features.h",
    "glibc-like/probe.c",
    "glibc-like/sys/cdefs.h",
    "glibc-like/sys/types.h",
]

[target_applicability."x86_64-unknown-linux-gnu"]
status = "applicable"
evidence_kind = "parse-only"
entrypoint = "glibc-like/probe.c"
reason = "The original fixture certifies preprocessing and parsing only and does not depend on target layout, linking, or execution."

[target_applicability."aarch64-unknown-linux-gnu"]
status = "applicable"
evidence_kind = "parse-only"
entrypoint = "glibc-like/probe.c"
reason = "The original fixture certifies preprocessing and parsing only and does not depend on target layout, linking, or execution."

[target_applicability."riscv64-unknown-linux-gnu"]
status = "applicable"
evidence_kind = "parse-only"
entrypoint = "glibc-like/probe.c"
reason = "The original fixture certifies preprocessing and parsing only and does not depend on target layout, linking, or execution."

[target_applicability."aarch64-apple-darwin"]
status = "applicable"
evidence_kind = "parse-only"
entrypoint = "glibc-like/probe.c"
reason = "The original fixture certifies preprocessing and parsing only and does not depend on target layout, linking, or execution."
