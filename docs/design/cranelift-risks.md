# Cranelift risk register

Each item records a backend mismatch and the behavior CCC requires. A feature is enabled for a target only when its capability test and cross-linked tests pass; otherwise CCC emits a hard diagnostic at the first semantic use.

## Risk register

| #   | Problem                                                       | Required strategy                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| --- | ------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | **C struct-by-value ABI**                                     | Per-target classifiers populate immutable boundary plans within the [`ModuleAbiPlan`](abi-and-varargs.md#module-abi-plan). Aggregate splitting, hidden returns, stack placement, and copy glue are tested against psABI references and cross-linked binaries.                                                                                                                                                                                                                                                                                                                           |
| 2   | **Variadic calls and definitions**                            | Do not rely on cg_clif's integer-only call workaround. Use native backend support when complete, otherwise generated call and callee-entry bridges that handle special registers, register-save areas, stack overflow areas, indirect calls, and the target `va_list`; see [ABI & variadics](abi-and-varargs.md).                                                                                                                                                                                                                                                                       |
| 3   | **f80 and binary128 `long double`**                           | Preserve the target representation. Binary128 uses verified `f128`/libcall lowering; x86 f80 uses runtime arithmetic plus assembly boundary bridges for x87 arguments and returns. Missing arithmetic or ABI support is a hard error; f64 substitution requires explicit compatibility mode.                                                                                                                                                                                                                                                                                            |
| 4   | **GNU inline assembly**                                       | The AST retains template, operands, constraints, alternatives, ties, early-clobber, clobbers, volatility, and goto labels. A side object is valid only when an explicit marshalling stub preserves those semantics. Stack-sensitive asm, `asm goto`, local control flow, or unsupported constraints require a whole-function assembly path or a hard error; they are never converted into an ordinary helper call.                                                                                                                                                                      |
| 5   | **Runtime-sized automatic storage and native-stack builtins** | ISO VLA objects use provider-neutral CCC-IR region effects. The selected hosted provider is a per-invocation scoped arena that leaves Cranelift's stack pointer fixed. Native-stack operations such as GNU `alloca` remain unavailable until a maintained backend facility participates in fixed slots, spills, calls, probes, debug data, and unwind information. An ordinary stack-mutating helper or thunk is never used. See [ADR-0011](../adr/0011-arena-backed-runtime-sized-automatic-storage.md).                                                                               |
| 6   | **TLS**                                                       | Map the effective TLS model to supported Cranelift/object relocations and validate linker results per format. Unsupported LE/IE/GD combinations call a documented resolver sequence or fail; `tls_value` existence alone is not proof of complete object/link support.                                                                                                                                                                                                                                                                                                                  |
| 7   | **C11 atomics**                                               | Preserve C orders in CCC-IR, conservatively map supported operations, and use `libatomic`/CCC helpers for unsupported widths or alignments. Lock-free queries and builtin-header macros use the same capability table.                                                                                                                                                                                                                                                                                                                                                                  |
| 8   | **Debug information**                                         | Source locations feed line tables; a CCC DWARF layer using gimli emits compilation units, types, scopes, variables, frame information, and relocation-bearing sections. Backend `SourceLoc` support is only the input mapping.                                                                                                                                                                                                                                                                                                                                                          |
| 9   | **PIC, PIE, and relocation models**                           | Select matching codegen and linker modes from the effective configuration. Probe the target driver default and explicitly disable PIE for non-PIE output; never mix incompatible relocations and driver defaults.                                                                                                                                                                                                                                                                                                                                                                       |
| 10  | **ELF vs Mach-O**                                             | Object-format adapters own section names, symbol/visibility rules, relocations, TLS, constructors, unwind sections, and shared-library naming. `cranelift-object` is verified by object inspection rather than assumed to cover every platform rule.                                                                                                                                                                                                                                                                                                                                    |
| 11  | **CPU features and SIMD**                                     | Normalize `-march`/`-mcpu`/`-m*` into one feature set used by predefined macros, type legality, ABI classification, and Cranelift ISA flags. A vector type or intrinsic is enabled only when all four agree.                                                                                                                                                                                                                                                                                                                                                                            |
| 12  | **`setjmp`/`longjmp`**                                        | Mark calls as returns-twice in CCC-IR. Materialize locals live across the call, preserve volatile locals, insert compiler/memory barriers, disable incompatible motion/tail-call transformations, and require a backend capability test proving register allocation and frame restoration. A function that also owns an arena needs an explicit checkpoint at the returns-twice boundary; until verified, diagnose that combination. A nonlocal exit across another invocation may strand its arena, as C permits VLA storage to be squandered, but cannot corrupt shared active state. |
| 13  | **Large frames and stack probes**                             | Compute static-frame obligations and use the target's native probe rules. Native dynamic-stack operations must compose with guard pages and stack-clash probes. Arena-backed automatic storage instead requires checked size/alignment, deterministic allocator failure, and memory-reuse tests; it does not satisfy native stack-usage or stack-clash claims. Probe symbols are resolved through the target runtime, not the host runtime.                                                                                                                                             |
| 14  | **Compiler-emitted helper calls**                             | A target runtime manifest maps every external helper signature and symbol to compiler-rt, libgcc, libatomic, libc, or a versioned CCC shim. Link-time availability is probed and helper ABI is covered by cross-linked tests. The arena provider's local CLIF support functions are primary-object definitions; their `malloc`, `free`, and non-returning `abort` dependencies are exact hosted-libc manifest entries.                                                                                                                                                                  |
| 15  | **`_Complex`**                                                | Expose the capability only with typed pair operations, floating-point edge-case tests, libc/header support, and per-target ABI plans. Otherwise advertise `__STDC_NO_COMPLEX__` and reject semantic use.                                                                                                                                                                                                                                                                                                                                                                                |
| 16  | **Optimization expectations**                                 | Each `-O` level has a documented pass set and predefined-macro behavior. Higher levels may share a pass set, but the driver does not claim an optimization or aliasing contract it does not implement.                                                                                                                                                                                                                                                                                                                                                                                  |
| 17  | **Computed goto and labels-as-values**                        | Cranelift has no indirect branch to arbitrary code addresses. `&&label` lowers to a per-function index over the address-taken labels and `goto *e` to a `br_table` dispatch on that index; indices are carried in pointer-typed values so storage and equality comparisons behave. Cross-function label values remain undefined behavior and are diagnosed where detectable. Lua's default GNU-profile build selects computed-goto VM dispatch, so the Lua corpus gate depends on this lowering.                                                                                        |

## Runtime-sized automatic storage contract

Runtime extents are evaluated once at the declaration and retained for runtime
`sizeof` and multidimensional pointer strides. The complete byte size uses
checked `size_t` arithmetic. Each declaration receives a provider mark and a
stable pointer aligned to the greater of its declared alignment and the target
VLA minimum. A nonpositive bound, arithmetic overflow, or provider failure
enters a non-returning failure path.

CCC-IR represents region entry, allocation, and restoration explicitly. Its
verifier requires identical active-region stacks at CFG merges, permits a
runtime-sized storage address only while its region is active, and requires
ordinary return edges to restore every active region. Semantic analysis rejects
named `goto` and switch ingress that bypass a variably modified declaration.
Lowering restores the active suffix on normal fallthrough, `break`, `continue`,
outward and backward `goto`, and return after evaluating the return operand.
Computed-goto dispatch must either preserve the same region state or select a
target-specific cleanup edge; it cannot enter a deeper or unrelated region.

The selected hosted provider is the per-invocation arena recorded by
[ADR-0011](../adr/0011-arena-backed-runtime-sized-automatic-storage.md). The
capability remains unavailable until semantic, IR, provider, link, failure,
leak, alignment, recursion, concurrency, and performance tests all pass.

## Native dynamic-stack capability contract

GNU stack-allocation builtins require a backend operation that takes a runtime
size and power-of-two alignment, allocates and restores native stack space, and
participates in fixed-slot and spill addressing, call alignment, target probes,
debug information, and unwind metadata. CCC will pursue that facility upstream.
It will not carry a private Cranelift fork or substitute an ordinary helper or
assembly thunk. Arena-backed ISO VLA support does not enable this capability.

## Returns-twice capability contract

Functions containing a returns-twice call use a conservative lowering profile: affected locals are memory-resident, memory-sensitive optimization is disabled across the call, frame pointers are retained where required, and no tail call crosses the boundary. Tests modify volatile and non-volatile locals around `setjmp`, perform nested calls, and cross-link against the system implementation at every optimization level. This profile is enabled only after emitted-code tests prove the pinned backend behavior.
